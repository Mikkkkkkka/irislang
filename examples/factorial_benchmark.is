процедура главная()
нч
    целч i = 0;
    целч сумма = 0;
    пока (i < 5000)
    нч
        сумма = сумма + факториал(20);
        i = i + 1;
    кц
    печать:сумма;
кц

функция:целч факториал(целч n)
нч
    если (n <= 1) нч
        вернуть 1;
    кц
    вернуть n * факториал(n - 1);
кц
