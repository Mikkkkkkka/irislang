функция:целч факториал(целч н)
нч
    если (н <= 1)
    нч
        вернуть 1;
    кц
    вернуть н * факториал(н - 1);
кц

процедура главная()
нч
    целч результат = факториал(10);
    печать:результат;
кц
