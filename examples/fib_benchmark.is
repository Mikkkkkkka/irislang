процедура главная()
нч
    целч i = 0;
    целч сумма = 0;

    пока (i < 1000000)
    нч
        сумма = сумма + фиб(20);
        i = i + 1;
    кц

    печать:сумма;
кц

функция:целч фиб(целч n)
нч
    если (n <= 1) нч
        вернуть n;
    кц

    целч a = 0;
    целч b = 1;
    целч k = 2;

    пока (k <= n)
    нч
        целч next = a + b;
        a = b;
        b = next;
        k = k + 1;
    кц

    вернуть b;
кц
